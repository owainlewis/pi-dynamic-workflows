# GitHub issue planning

You are preparing a safe demo workflow from a GitHub issue.

Issue reference:

{{ .Task }}

Run directory:

{{ .RunDir }}

Do this:

1. Read `{{ .RunDir }}/ISSUE.json`.
2. Use `gh issue view "{{ .Task }}" --comments` to understand the issue.
3. Inspect the repository enough to identify the likely files and checks.
4. Write `{{ .RunDir }}/ISSUE.md` with the issue title, URL, summary, acceptance criteria, and relevant comments.
5. Write `{{ .RunDir }}/PLAN.md` with a small implementation plan, risks, files likely to change, and validation steps.
6. Write `{{ .RunDir }}/PR_BODY.md` with a draft PR body containing summary, test plan, and a note that this was generated by the Flow demo.

Do not modify source files in this step.
